# 六智能体截图与录屏材料设计

## 目标

在保留 `D:\矿业\图片.docx` 原有内容的前提下，追加六智能体预警关键截图、真实性说明、完整录屏流程和可直接后期配音的逐段文字稿。

## 内容结构

1. 六智能体预警新增功能概述。
2. 四张真实运行截图：板块总览、完整六节点结果、A1 XGBoost证据、A4人工确认边界。
3. 录屏前准备清单。
4. 约8至10分钟的完整分镜表。
5. 与分镜逐段对应的后期配音稿。
6. 故障恢复与真实性口径。

## 截图原则

- 使用本地最新版项目和真实服务器返回结果，不使用静态示意图替代运行结果。
- 截图显示 `teacher_real_csv_xgboost`、真实记录号、A1至A6顺序和A4等待人工确认。
- 图片宽度适配Word版心，不拉伸，不裁掉状态和安全边界。
- 图注解释“看到了什么”和“证明了什么”，不重复界面文字。

## 录屏叙事

主线为：统一登录与角色权限 → 真实数据与XGBoost算法预警 → Three.js风险定位 → 企业端处置 → 监管端驳回 → 企业端补充 → 监管端通过 → 智库端六智能体研判 → DeepSeek专家建议 → 闭环归档。

算法预警、六智能体研判和三端人员协同分别说明，避免将三个登录角色称为三个Agent。四分钟后的45分记录只作为真实监测对照，不作为处置因果证明。

## 验收

- 原Word内容和原24张图片保留。
- 追加图片可读且图注完整。
- 录屏流程包含时间、画面操作、配音、检查点和失败备用路线。
- 文档结构检查无缺失，并完成渲染后的逐页视觉检查。
