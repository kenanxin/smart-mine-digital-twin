{
  "name": "smart-mine-v2-balanced",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "preflight": "node tools/submission-preflight.mjs",
    "preflight:offline": "node tools/submission-preflight.mjs --offline",
    "package:submission": "powershell -NoProfile -ExecutionPolicy Bypass -File tools/build-submission-package.ps1"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "echarts": "^6.1.0",
    "three": "^0.160.0"
  }
}
